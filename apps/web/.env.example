# URL del backend (apps/api). En local: http://localhost:4000
# En Vercel: se configura como variable de entorno del proyecto,
# apuntando a la URL pública del servicio de Render.
VITE_API_URL=http://localhost:4000
